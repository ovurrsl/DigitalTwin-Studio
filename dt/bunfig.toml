[install]
linker = "hoisted"
# react, three, next and @pascal-app/* come from the upstream workspace install at
# the repo root (node resolution walks up to it); installing peers here would
# create second copies of them.
peer = false
